export const publicRoutes = ["/", "/verification"];

export const authRoutes = ["/error", "/login", "/register"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/settings";
